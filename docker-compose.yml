# Placeholder Docker Compose file for future services.
# For now, only Redis is enabled for Celery broker/result backend.
# Planned services to add later:
# - backend
# - frontend
# - postgres
# - worker

services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"
